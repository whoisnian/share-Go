{
  "name": "share-fe",
  "version": "0.1.11",
  "description": "frontend of share-Go",
  "author": "whoisnian",
  "license": "MIT",
  "scripts": {
    "start": "NODE_ENV=development node ./scripts/start.js",
    "build": "rm -rf ./dist && NODE_ENV=production node ./scripts/build.js",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "test": "NODE_ENV=test node --test"
  },
  "type": "module",
  "engines": {
    "node": ">=20.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.8",
    "eslint": "^9.31.0",
    "globals": "^16.3.0",
    "neostandard": "^0.12.2"
  }
}